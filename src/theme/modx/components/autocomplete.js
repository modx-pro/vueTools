/**
 * AutoComplete tokens
 *
 * Dropdown button width and chip radius follow the manager field trigger and
 * superbox select items.
 */

export const dropdown = {
  width: '1.875rem',
  sm: {
    width: '1.625rem'
  },
  lg: {
    width: '2.25rem'
  }
}

export const chip = {
  borderRadius: '{border.radius.xs}'
}

export default {
  dropdown,
  chip
}
