/**
 * MultiSelect tokens
 *
 * Chips follow the manager superbox select items: minimal radius, tight
 * padding (`.x-superboxselect-item` uses 4px).
 */

export const dropdown = {
  width: '1.875rem',
  color: '{form.field.icon.color}'
}

export const chip = {
  borderRadius: '{border.radius.xs}'
}

export const option = {
  gap: '0.375rem'
}

export default {
  dropdown,
  chip,
  option
}
