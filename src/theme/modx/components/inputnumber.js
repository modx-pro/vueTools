/**
 * InputNumber tokens
 *
 * Spinner buttons reuse the manager field trigger width (30px).
 */

export const button = {
  width: '1.875rem',
  verticalPadding: '0.25rem'
}

export default {
  button
}
