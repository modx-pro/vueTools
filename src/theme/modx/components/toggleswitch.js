/**
 * ToggleSwitch tokens
 *
 * Compact version of the manager slider control from `_forms.scss`. The track
 * stays pill shaped because that is what a switch is; only the size shrinks.
 */

export const root = {
  width: '2rem',
  height: '1.125rem',
  borderRadius: '30px',
  gap: '0.125rem',
  borderWidth: '1px'
}

export const handle = {
  borderRadius: '50%',
  size: '0.875rem'
}

export default {
  root,
  handle
}
