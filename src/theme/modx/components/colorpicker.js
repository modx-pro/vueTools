/**
 * ColorPicker tokens
 *
 * The preview swatch takes the form field height so it aligns with inputs in
 * a row.
 */

export const preview = {
  width: '2rem',
  height: '2rem',
  borderRadius: '{form.field.border.radius}'
}

export default {
  preview
}
